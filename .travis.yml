sudo: required

# Install Docker
services:
  - docker

# Execute logic
script:
  - echo "Hello Everyone!"

after_success:
  # Build all the docker files
  - docker build -t mshravan/zomato-shapeai-client ./client
  - docker build -t mshravan/zomato-shapeai-server ./server
  - docker build -t mshravan/zomato-shapeai-nginx ./nginx

  # log in docker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

  # push to docker hub
  - docker push mshravan/zomato-shapeai-client
  - docker push mshravan/zomato-shapeai-server
  - docker push mshravan/zomato-shapeai-nginx